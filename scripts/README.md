# Scripts

This directory contains Python scripts for repeatable, automatable operations.

Scripts are meant to be run the same way every time, by any team member, without modification. This is your pipeline code — it should be clean, documented, and runnable from the command line.

**Examples:** `clean_data.py`, `run_analysis.py`, `generate_report.py`
