# Processed Data

This directory contains cleaned and transformed data ready for analysis.

Every script and notebook reads from here, not from raw. Any team member can regenerate processed data from raw using your cleaning scripts — which means processed files are reproducible and do not need to be committed if they are large.

**Rule:** Only use data from this directory for analysis. Regenerate from raw when needed.
