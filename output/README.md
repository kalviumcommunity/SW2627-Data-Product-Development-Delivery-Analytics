# Output

This directory contains generated files — reports, figures, predictions, exports.

Output is always regenerable from your scripts and data. Never treat output as source-of-truth — treat the scripts that produce it as source-of-truth. Large output files are typically gitignored.

**Rule:** Regenerate output from scripts. Never edit files here directly.
